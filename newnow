# 用 Python 的 aware datetime，然後轉成 EWSDateTime
since_py = datetime.now(timezone.utc) - timedelta(days=LOOKBACK_DAYS)
since    = EWSDateTime.from_datetime(since_py)

qs = folder.filter(datetime_received__gte=since).order_by("-datetime_received")