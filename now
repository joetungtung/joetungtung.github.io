def to_ts(s: pd.Series) -> pd.Series:
    # 嘗試用標準格式一次轉換；若失敗再 fallback
    try:
        return pd.to_datetime(s, format="%Y-%m-%d %H:%M:%S", errors="coerce", utc=True)
    except Exception:
        return pd.to_datetime(s, errors="coerce", utc=True)