from exchangelib import EWSTimeZone, EWSDateTime

# 建立 Exchange 可接受的 UTC 時區
UTC_TZ = EWSTimeZone.timezone('UTC')

since = EWSDateTime.now(tz=UTC_TZ) - timedelta(days=LOOKBACK_DAYS)
qs = folder.filter(datetime_received__gte=since).order_by("-datetime_received")